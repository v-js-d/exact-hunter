DOCKER_IMAGE=
NODE_ENV=
PORT=

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=

CHOKIDAR_USEPOLLING=
WATCHPACK_POLLING=

# API: mock — MSW перехватывает запросы; real (или значение не указано) — запросы идут на NEXT_PUBLIC_API_URL
NEXT_PUBLIC_API_MODE=real
